# Test-only dependencies. Deliberately not in requirements.txt, which is what
# the images install: none of this belongs in a running container.
#
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest
httpx      # required by fastapi.testclient.TestClient
